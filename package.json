{
  "name": "@cloudwell/simple-dialog",
  "version": "0.0.0-rc",
  "description": "A Fluent UI Dialog wrapper with implementations to replace the `alert`, `confirm`, and `prompt` functions.",
  "main": "index.js",
  "readme": "README.md",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc && cp package.json README.md ./dist",
    "deploy": "npm run build && npm publish ./dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Cloudwell-IO/simple-dialog.git"
  },
  "author": "Cloudwell",
  "license": "GPL-3.0",
  "bugs": {
    "url": "https://github.com/Cloudwell-IO/simple-dialog/issues"
  },
  "homepage": "https://github.com/Cloudwell-IO/simple-dialog#readme",
  "peerDependencies": {
    "@fluentui/react": "^8.70.0",
    "@types/react": "^16.8.0",
    "react": "^16.8.0",
    "react-dom": "^16.8.0",
    "typescript": "^4.6.4"
  }
}
